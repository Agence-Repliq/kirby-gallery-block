name: field.blocks.gallery.name
icon: dashboard
preview: gallery
fields:
  layout:
    type: toggles
    label: Nombres d'image par ligne
    width: 1/3
    options:
      - 2
      - 3
      - 4

  gallery:
    label: field.blocks.gallery.images.label
    type: structure
    fields:
      image:
        label: image
        type: files
        mutliples: false
        query: page.images
      alt:
        label: field.blocks.image.alt
        type: text
        icon: title
        help: Texte permettant de comprendre l'image sans la voir. C'est pour le référencement et les personnes porteuses de handicaps.

  caption:
    label: field.blocks.image.caption
    type: writer
    inline: true
    nodes: false
    marks:
      - bold
      - strike
      - link
    icon: text
    help: S'affiche sous la photo
    inline: true
    